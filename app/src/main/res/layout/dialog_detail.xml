<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/rounded_full_white_40dp">

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/tvStatusPresence"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hadir"
                android:textSize="30sp"
                android:textColor="@color/black"/>

            <TextView
                android:id="@+id/tvTanggal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Senin, 25 Mei 2019"
                android:textSize="14sp"
                android:layout_below="@id/tvStatusPresence"
                android:textColor="@color/greytextcolor"/>

            <TextView
                android:id="@+id/tvDeskripsi"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hadir selalu"
                android:textSize="14sp"
                android:layout_below="@id/tvTanggal"
                android:textColor="@color/greytextcolor"/>


        </RelativeLayout>

    </ScrollView>

    <Button
        android:id="@+id/btnOk"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:text="Ok"
        android:layout_marginTop="15dp"
        android:background="@drawable/btn_silver"
        android:layout_below="@id/scrollView"
        android:layout_alignParentEnd="true"
        />
</RelativeLayout>